[pytest]
pythonpath = .
testpaths = tests
# -v: show each test name; -ra: end-of-run summary with reasons for skips/fails;
# --tb=short: concise failure tracebacks.
addopts = -v -ra --tb=short
markers =
    live: end-to-end tests that call the real API (deselect with -m "not live")
